{
  "name": "n8n-project-1",
  "version": "1.0.0",
  "description": "Unified root package.json for n8n project",
  "main": "server.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "install-all": "npm install --prefix ./Agent && npm install --prefix ./frontend && npm install --prefix ./proxy"
  },
  "author": "pushparani",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^0.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}

